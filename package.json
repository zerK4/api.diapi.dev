{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun src/scripts/sync.ts && bun run src/index.ts",
    "format": "prettier --write .",
    "turso:local": "npx turso dev --db-file database.db",
    "push:db": "npx drizzle-kit push:sqlite",
    "migration:generate": "npx drizzle-kit generate:sqlite",
    "studio": "npx drizzle-kit studio"
  },
  "dependencies": {
    "@libsql/client": "^0.6.0",
    "@tursodatabase/api": "^1.4.0",
    "@types/uuid": "^9.0.8",
    "drizzle-orm": "^0.30.7",
    "hono": "^4.2.3",
    "prettier": "^3.2.5",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.20.14"
  }
}